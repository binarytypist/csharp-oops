namespace Bakery.Utilities.Enums
{

    // Enumeration representing the types of tables available in the bakery.
    // The TableType enum defines two values: InsideTable and OutsideTable, which correspond
    // to the types of tables that can be added to the bakery.
    public enum TableType
    {
        InsideTable = 1,
        OutsideTable = 2
    }
}